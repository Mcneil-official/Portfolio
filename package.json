{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Light-themed developer portfolio built with Tailwind CSS",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o styles.css --minify",
    "watch": "tailwindcss -i input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}